#include "main.h"
/**
*_isupper - check if a letter is upper
*@x: The number to be checked
*Return: 1 for upper letter
*/
int _isupper(int c)
{
if (c >= '0' && x <= '0')
{
return (1);
}
return (0);
}
