#include "main.h"
/**
*_isupper - check if a letter is upper
*@c: The number to be checked
*Return: 1 for upper letter
*/
int _isupper(int c)
{
if (c >= '0' && c <= '0')

return (1);
else
return (0);
}
